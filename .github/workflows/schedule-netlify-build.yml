name: Schedule Netlify Build
on:
  schedule:
       - cron: '32 18 * * 2,5,0' 
jobs:
  build:
    name: Call Netlify build hook
    runs-on: ubuntu-latest
    steps:
      - name: Curl request
        run: curl -X POST -d {https://api.netlify.com/build_hooks/630b9231fbefa961d3c7102f} 
